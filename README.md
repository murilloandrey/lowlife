# lowlife

Lowlife e-commerce

## Contributing

Create `feature/<short-description>` branches from `develop`.
Open feature pull requests into `develop`.
All CI checks must pass before merge.
For a release, open a pull request from `develop` into `main`.
Do not merge feature branches directly into `main`.
